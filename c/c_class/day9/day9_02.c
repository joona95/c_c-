#include <stdlib.h>
int main()
{




    /*
    char *buf;
    char a[4]={20,40,30,10};
    buf = (char *)malloc(sizeof(char)*200);
    if(!buf)  //buf == NULL
        return;  //main()함수 종료
    memcpy(buf,a,sizeof(char)*4);  //a: 원본의 시작주소  buf: 복사할 메모리 장소, memcpy = memory copy
    printf("%d %d %d %d \n", buf[0], buf[1], buf[2],buf[3]);

    free(buf);
    */



    /*
    int* p = NULL;
    p = (int *)malloc(4);

    if(p == NULL)
        printf("힙 영역에 동적 메모리 할당 실패 \n");  //에러처리, 예외처리
    *p = 10;
    printf("주소 : %x \n", p);
    printf("값 : %d", *p);

    free(p);



    return 0;
    */
}
